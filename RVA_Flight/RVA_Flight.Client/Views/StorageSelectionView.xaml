<UserControl x:Class="RVA_Flight.Client.Views.StorageSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RVA_Flight.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">

    <Grid Background="#f5f7fa">
        <Border Padding="30"
                CornerRadius="12"
                Background="White"
                Width="300"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                >
            <StackPanel>
                <TextBlock Text="Select storage option"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0,0,0,15"
                           HorizontalAlignment="Center"/>

                <ComboBox ItemsSource="{Binding StorageOptions}" 
                          SelectedItem="{Binding SelectedStorage}" 
                          Width="200"
                          Height="30"
                          Margin="0,0,0,15"
                          HorizontalAlignment="Center"/>

                <Button Content="Confirm Selection"
                        Command="{Binding SelectStorageCommand}"
                        Width="200"
                        Height="35"
                        HorizontalAlignment="Center"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderBrush="#388E3C"
                        BorderThickness="1"
                        
                        Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
