<UserControl x:Class="RVA_Flight.Client.Views.FlightStateSimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RVA_Flight.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <DataGrid ItemsSource="{Binding SimulatedFlights}" AutoGenerateColumns="False" Height="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Flight Number" Binding="{Binding FlightNumber}" />
                <DataGridTextColumn Header="State" Binding="{Binding StateName}" />
                <DataGridTextColumn Header="Delay Minutes" Binding="{Binding DelayMinutes}" />
                <DataGridTextColumn Header="Pilot Message" Binding="{Binding PilotMessage}" />
            </DataGrid.Columns>
        </DataGrid>
            <StackPanel Orientation="Horizontal">
                <Button Content="START" Width ="80" Margin="5" Command="{Binding StartSimulationCommand}" HorizontalAlignment="Left"/>
                <Button Content="STOP" Width ="80" Margin="5" Command="{Binding StopSimulationCommand}" HorizontalAlignment="Left"/>
               
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
