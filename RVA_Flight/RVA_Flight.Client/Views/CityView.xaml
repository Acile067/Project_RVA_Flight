<UserControl x:Class="RVA_Flight.Client.Views.CityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RVA_Flight.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">
    <Grid Margin="10">
        <StackPanel>
            <DataGrid ItemsSource="{Binding Cities}" 
                      AutoGenerateColumns="False" 
                      Height="200" 
                      Margin="0,0,0,10" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      CanUserResizeRows="False" 
                      CanUserReorderColumns="False" 
                      CanUserResizeColumns="False" 
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Label Content="Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="150" Margin="0,0,5,0" Text="{Binding NewCity.Name, UpdateSourceTrigger=PropertyChanged}"/>
                <Label Content="Country:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="150" Margin="0,0,5,0" Text="{Binding NewCity.Country, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Add City" Command="{Binding AddCityCommand}" />
            </StackPanel>

            <TextBlock Text="{Binding ErrorMessage}" 
               Foreground="Red" 
               FontWeight="Bold" 
               Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</UserControl>
